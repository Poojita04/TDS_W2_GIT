name: Test Action

on:
  push:
    branches:
      - main  # or any branch you want to trigger the action

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: "23f1002880@ds.study.iitm.ac.in"  # Wrap the name in quotes
        run: echo "Hello, world!"  # Add space after `run:`
